<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.furnico">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Furnico"
        android:usesCleartextTraffic="true">
        <activity android:name=".EditUserDetailsActivity">

        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity android:name=".OrderPaymentActivity" />
        <activity
            android:name=".AccountOptions"
            android:label="Account Options"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity
            android:name=".UserDetails"
            android:label="User Details"></activity>
        <activity android:name=".CartActivity"></activity>
        <activity
            android:name=".DisplayProductsActivity"
            android:label="MyProducts"></activity>
        <activity android:name=".MyOrderActivity"></activity>
        <activity android:name=".ProductActivity2"></activity>
    </application>

</manifest>